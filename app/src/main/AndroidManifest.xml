<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mobapde.royalrumble">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/GameTheme">
        <activity android:name=".TitleActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TicTacToeLocalActivity" />
        <activity android:name=".GameMenuActivity" />
        <activity
            android:name=".MenuActivity"
            android:launchMode="singleTop" />
        <activity android:name=".unused.CheckersActivity" />
        <activity android:name=".OptionsActivity" />
        <activity android:name=".CreditsActivity" />
        <activity android:name=".unused.RPSActivity" />
        <service android:enabled="true" android:name=".service.BackgroundSoundService" />

        <uses-feature android:name="android.hardware.camera"
            android:required="true" />
        <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
        <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    </application>



</manifest>